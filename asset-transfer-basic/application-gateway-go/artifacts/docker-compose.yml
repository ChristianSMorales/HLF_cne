version: '3.7'

services:
  ipfs:
    image: ipfs/go-ipfs:latest
    ports:
      - "4001:4001"
      - "5001:5001"
      - "8081:8081"
    volumes:
      - ipfs-data:/data/ipfs

volumes:
  ipfs-data:
